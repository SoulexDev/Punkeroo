public enum PlayerState { Idle, Run, Jump, Fall, Dash }
public enum DungeonRoomHorizontalState { HorizontalRight, HorizontalLeft, Both }
public enum DungeonRoomVerticalState { VerticalUp, VerticalDown, Both }
public enum Direction { Up = 0, Down = 1, Left = 2, Right = 3 }